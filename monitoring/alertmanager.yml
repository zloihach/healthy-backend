global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'email'

receivers:
  - name: 'email'
    email_configs:
      - to: 'homkkarsx@gmail.com'
        from: 'zloezlowork@gmail.com'
        smarthost: 'smtp.gmail.com:465'
        auth_username: 'Healthy | Alert Manager'
        auth_password: 'hrldaeejfqmnuhnt'
